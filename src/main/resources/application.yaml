spring:
  application:
    name: generic-product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: mypass
    username: postgres
    hikari:
      schema: product
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8083