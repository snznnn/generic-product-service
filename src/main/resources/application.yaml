spring:
  application:
    name: generic-product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: mypass
    username: postgres
    hikari:
      schema: product
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8083